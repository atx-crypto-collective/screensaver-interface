import React from 'react'
import { Meta } from '@storybook/react'

import { Layout } from '../../components'

export const Default = () => <Layout>Contents</Layout>

export default {
  title: 'Components/Layout',
  component: Layout,
} as Meta
